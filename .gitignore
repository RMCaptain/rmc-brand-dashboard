node_modules/
.env
.env.*
*.env
google-service-account.json

# Runtime artifacts
server.log
logs/
pm_check.json

# Puppeteer browser download (installed at build time, ~300MB — not source)
.cache/

# Data cache (generated, not source of truth)
data/fx.json
data/listings-cache.json
data/image-cache.json
data/live-metrics.json
data/metrics.json
# Business data — lives in Supabase; never in version control (audit F11)
data/brands.json
data/preset-metrics.json

# Daily audit run history (regenerated each day)
audit/history/
